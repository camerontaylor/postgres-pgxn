services:
    - docker

script:
  - docker build -t zitsen/postgres-pgxn .

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
      docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
      docker push zitsen/postgres-pgxn;
    fi
